import { imageDetails } from "@/src/content/investigation";
export function ImageInspectSheet({ imageId, close, inspect }: { imageId: string; close: () => void; inspect: (region: string) => void }) { const image = imageDetails[imageId]; return <div className="sheet-backdrop" onClick={close}><section className="sheet" role="dialog" aria-label="原图详情" onClick={(e) => e.stopPropagation()}><div className="sheet-head"><h2>{image?.title ?? "原图详情"}</h2><button className="ghost" onClick={close}>关闭</button></div>{image?.details.map((detail, index) => <button className="saved-entry" key={detail} onClick={() => inspect(String(index))}>{detail}</button>)}</section></div>; }
