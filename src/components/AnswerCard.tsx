import { question } from "@/src/content/p01";
import type { GameState } from "@/src/game/types";

export function AnswerCard({ state, onRead, onComments, onAuthorChat, onProfile, onEditHistory }: { state: GameState; onRead: () => void; onComments: () => void; onAuthorChat?: () => void; onProfile?: () => void; onEditHistory?: () => void }) {
  return <section className={`card answer-card ${state.currentRun.hasReadP01Answer ? "answer-read" : ""}`}><div className="author-row"><button className="avatar" onClick={onProfile}>南</button><div><button className="link-button" onClick={onProfile}><strong>{question.author}</strong></button><div className="meta">{question.authorSignature} · {question.publishedAt} · <button className="link-button" onClick={onEditHistory}>第5次编辑</button></div></div><button className="link-button" onClick={onAuthorChat}>私信</button></div><div className={`answer ${state.currentRun.hasReadP01Answer ? "expanded" : "excerpt"}`}>{question.answer}</div>{state.currentRun.hasReadP01Answer && <p className="answer-reveal">{question.answerTail}</p>}{!state.currentRun.hasReadP01Answer && <button className="link-button" onClick={onRead}>展开阅读全文</button>}<div className="post-actions"><button className="plain-action">👍 赞同 1.2K</button><button className="plain-action" onClick={onComments}>💬 24 条评论</button><button className="plain-action">收藏</button><button className="plain-action">分享</button></div></section>;
}
