"use client";
import { useState } from "react";
import { question, comments } from "@/src/content/p01";
import { useGameStore } from "@/src/store/gameStore";
const materialLabels = ["404", "住宿登记表", "原回答改写", "编辑记录", "宋砚旧便签"];
export function StoryV2() { const state = useGameStore(); const [text,setText]=useState(""); const r=state.currentRun; const send=(type:string,extra={})=>state.dispatch({type,actionId:`v2-${Date.now()}-${Math.random()}`,...extra} as never); return <><header className="topbar"><div className="topbar-inner"><strong className="brand">知境</strong><span className="time">现实时间：2026-09-07</span></div></header><main className="layout"><div><section className="post-header"><div className="meta">知乎 · 问题</div><h1 className="title">{question.title}</h1><div className="meta">328 人关注 · 126,731 次浏览 · 118 个回答</div></section><article className="card answer-card"><div className="author-row"><div className="avatar">南</div><div><strong>{question.author}</strong><div className="meta">{question.authorSignature}</div></div></div><div className="answer expanded">{question.answer}</div><p className="answer-reveal">{question.answerTail}</p><div className="meta">编辑于 3 天前</div></article>{r.v2Phase>=1&&<section className="card comments-card"><h3>评论</h3>{comments.map((c,i)=><div className="comment" key={c}><div className="comment-avatar">{["山","折","晚","—"][i]}</div><div><strong>{["山楂汽水","折叠椅","晚八不点名","你"][i]}</strong><div>{c}</div><small>3 天前</small></div></div>)}{r.v2PlayerComment&&<><div className="comment"><div className="comment-avatar">你</div><div><strong>你</strong><div>{r.v2PlayerComment}</div><small>刚刚</small></div></div><div className="comment anomaly"><div className="comment-avatar">南</div><div><strong>{question.author} 回复 你</strong><div>终于有人回了。<br/>你那边现在几点？</div><small>3 天前</small></div></div></>}{!r.v2AuthorReply&&<form onSubmit={e=>{e.preventDefault();send("V2_REPLY",{text});setText("")}}><textarea aria-label="写下你的评论" value={text} onChange={e=>setText(e.target.value)} placeholder="写下你的评论……"/><button type="submit">回复</button></form>}</section>}{r.v2Phase===0&&<button onClick={()=>send("V2_READ_ANSWER")}>阅读完整回答</button>}{r.v2Phase===2&&<section className="card"><h3>确认现实时间</h3><p>这条回复的时间早于你的评论。现在现实日期是？</p><button onClick={()=>send("V2_CONFIRM_DATE")}>2026 年 9 月 7 日</button></section>}{r.v2Phase>=3&&<section className="card"><h3>来自 3 天前</h3><p>南楼旧床板</p><p>我还在通道里。门牌后面有一张旧便签，写着宋砚的名字。</p><div className="actions">{materialLabels.map(m=><button key={m} className={r.v2Materials.includes(m)?"secondary":""} onClick={()=>send("V2_VIEW_MATERIAL",{material:m})}>{r.v2Materials.includes(m)?"已看：":"查看："}{m}</button>)}</div>{r.v2Materials.length>=5&&<div className="actions"><button onClick={()=>send("V2_CHOOSE_ENDING",{ending:"END_A"})}>留下四个人名字</button><button onClick={()=>send("V2_CHOOSE_ENDING",{ending:"END_B"})}>让陈渡先走</button></div>}</section>}{r.v2Ending&&<section className="card"><h2>{r.v2Ending === "END_A" ? "END_A · 四个人" : "END_B · 回来一个"}</h2><p>{r.v2Ending === "END_A" ? "你把四个人的名字留在登记表上。" : "你让陈渡先走，通道里只回来一个人。"}</p></section>}</div><aside className="side"><section className="card"><h3>已看材料</h3>{r.v2Materials.map(m=><div className="related-item" key={m}><strong>{m}</strong></div>)}</section></aside></main></> }
