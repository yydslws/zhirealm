export function CommunityHeader({ onMessages, onSearch, unread = false }: { onMessages: () => void; onSearch?: (query: string) => void; unread?: boolean }) {
  return <header className="topbar"><form className="topbar-inner" onSubmit={(e) => { e.preventDefault(); const value = new FormData(e.currentTarget).get("query"); if (typeof value === "string" && value.trim()) onSearch?.(value.trim()); }}><div className="brand">知境</div><input name="query" className="search" aria-label="搜索问题" placeholder="搜索问题……" /><div className="header-actions"><button type="button" className="icon-button" aria-label="打开私信" onClick={onMessages}>💬{unread && <i className="unread-dot" />}</button><span className="avatar" aria-label="个人头像">知</span></div></form></header>;
}
