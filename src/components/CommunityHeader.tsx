export function CommunityHeader({ onMessages, onHome, onExitReading, onSearch, time = "01:57", unread = false }: { onMessages: () => void; onHome?: () => void; onExitReading?: () => void; onSearch?: (query: string) => void; time?: string; unread?: boolean }) {
  return <header className="topbar"><form className="topbar-inner" onSubmit={(e) => { e.preventDefault(); const value = new FormData(e.currentTarget).get("query"); if (typeof value === "string" && value.trim()) onSearch?.(value.trim()); }}><button type="button" className="brand" onClick={onHome}>知境</button><input name="query" className="search" aria-label="搜索问题" placeholder="搜索问题……" /><span className="time" aria-label="当前时间">{time}</span>{onExitReading && <button type="button" className="ghost" onClick={onExitReading}>退出阅读</button>}<div className="header-actions"><button type="button" className="icon-button" aria-label="打开私信" onClick={onMessages}>💬{unread && <i className="unread-dot" />}</button><span className="avatar" aria-label="个人头像">知</span></div></form></header>;
}
