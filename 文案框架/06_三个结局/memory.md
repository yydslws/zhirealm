# 06｜三个结局：memory

## 本轮工作记录

- 读取了技术文档、文案框架总览、00 总纲与接力约定、04 规则骨架、01—05 已交付文案与交互说明、交接记录，以及本目录 README。
- 只在 `文案框架/06_三个结局` 内新增交付文件，保留原 README。
- 完成 `完整文案.md`：定义发布后的共同提示、A/B/C 三条入口、确认与取消、2—3 屏收束、重来和本轮记录。
- 完成 `交互说明.md`：定义共同前置、本轮有效回答校验、结局事件、一次性结算、B 自动评论落点与计数、精简跨轮事实摘要字段。
- 完成 `交接记录.md`：面向 07/09 交代结局事实、字段、依赖和自审结果。

## 关键决定

- 三结局都要求 `hasPublishedOwnAnswer=true`、`ownAnswerRegistered=true`、`ownAnswerId` 当前周目有效。
- A 是宿管站内关闭确认，结局 ID 为 `death_404`；回答仍在，绑定仍在。
- B 是用户不存在固定话题“怎样离开这里？”提供的回首页确认，结局 ID 为 `exit`；临时离开，回答保留。新增评论落在自己的回答卡下方，问题回答数 119，高赞回答评论 25，自己的回答评论 1。
- C 是自己的回答卡“更多→删除回答”确认，结局 ID 为 `delete`；真实删除本轮回答并解除绑定，不需要 C5、全收集或信任门槛，且不把被删回答全文写进跨轮摘要。
- C 删除后 `ownAnswerRegistered=false`；B 的“怎样离开这里？”算真实回复；06 不补写未知旧回答的周目归属。
- 三个结局用 `P06_CTRL_CONTINUE_ENDING001` 在收束屏之间推进，第 3 屏后开放查看记录和重新进入。
- “但记录仍然存在”解释为来访痕迹，不否定 C 的解绑。
- 结局确认成功时一次性写精简 `previousRun` 事实摘要；缺失写 `unknown`，不使用 AI/NPC 闲聊摘要补历史。
- C 删除成功后清空本轮 `ownAnswerText`、草稿快照、发布快照及任何可恢复成活动回答的正文副本；跨轮不保存被删回答全文，`previousRun.ownAnswerId` 写 `null`。

## 自审

- 未扩写新分支、新 NPC 或 C5 门槛。
- 没有把聊天写成相信宿管。
- 没有把真实关闭浏览器标签页写成死亡。
- 没有让用户不存在知道删除出口。
- 没有把 B 自动评论写成玩家发送。
- 玩家正文不显示开发字段或三按钮选择题模板。
