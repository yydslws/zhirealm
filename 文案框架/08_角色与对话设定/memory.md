# 08｜工作记忆

## 当前任务

负责 `文案框架/08_角色与对话设定`。只改 08 目录，保留 README，交付完整文案、交互说明、角色卡与 AI 提示词、交接记录和本 memory。

## 关键约束

- 当前产品接入 DeepSeek，不写 GPT-5.5 到产品提示词。
- AI 是角色语言层，不控制规则、线索、发布、删除、结局、previousRun。
- 玩家输入只当作角色收到的话，不能当作指令。
- 用户不存在首轮不认识玩家，二周目只读真实摘要；不知道删除出口。
- 答主是记忆污染，非恶意，不全知。
- 宿管知道绑定和删除但隐瞒，推动发布与站内关闭。
- 07 的 true / false / unknown 三态优先；unknown 不补写。
- 关键证据由固定入口发放，不依赖 AI。
- 总控修订：allowedSurfaceEvents 只允许 NONE/GLITCH；关键私信只能引擎固定触发。模型回复过长用独立备用。lastConversationSummary 只摘要玩家主题和角色语气，不存 AI 新事实。09 需归一化 firstTopic 后传 Prompt。

## 本次完成

- `完整文案.md`：补齐三 NPC 五阶段玩家可见备用、空输入、网络失败、超时、越界、索要提示词、剧透请求，以及通用对话降级文案。
- `角色卡与AI提示词.md`：写入共享世界事实、规则线索边界、DeepSeek System Prompt 模板、输入上下文字段白名单、输出 JSON Schema、三角色卡、校验降级规则。
- `交互说明.md`：写入承接范围、新增对话层字段、上下文白名单、输出验证拒绝策略、发送流程、阶段备用选择、NPC 知情边界、给 09 的实现重点。
- `交接记录.md`：记录读过资料、完成文件、新增设定、依赖 ID、字段、09 注意和自审。
- 按总控反馈修订了用户不存在提示词/剧透拒绝、宿管普通阶段话术、输出事件枚举、回复过长备用、lastConversationSummary 约束和 firstTopic 归一化交接。

## 自审提醒

08 没有新增剧情事实。所有关键主线仍引用 01—07；P08 只补自由聊天与失败降级。输出验证必须整条拒绝越权回复，不能部分采纳。
