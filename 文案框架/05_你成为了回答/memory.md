# 05｜工作记忆

本目录由第 05 位负责。已按总纲、接力约定、04 规则骨架和 01—04 已完成成果补齐 05 的完整文案、交互说明与交接记录。未读取前序目录的 `memory.md`。

关键决定：

- 05 的核心动作是“查看草稿”和“明确确认发布”，不是结局选择。
- 草稿由基础正文与 4 条证据型条件插句拼接，快照在打开草稿时生成，发布确认时冻结。发布后的正文必须与确认框内文本一致。
- 只有实际看过 C2 或 C4 才能进入 05；发布前保持 `timeStopped=false`、`gameTime=02:07`、`pollution=2`。
- 发布成功后新增唯一署名“你”的回答卡，与“南楼旧床板”原卡并存，并写入 `timeStopped=true`、`pollution=3`、`phase06Available=true`。
- 发布后自己的“更多→删除回答”入口始终可用，不能被 C5 或全收集门槛隐藏。
- 暂不发布、关闭草稿、取消发布确认、真实刷新都无惩罚，不自动发布、不新增周目、不触发结局。
